import React from 'react';
import image from "./logo.jpg";

const Logo = ({  }) => {
   
  return (
    
   <div id="logo">
    <img src={image} alt="Logo"></img>
    </div>
  );
}


export default Logo;